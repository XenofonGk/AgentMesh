/**
 * Drizzle schema.
 *
 * Deliberately empty at Phase 0. The first tables land in Phase 1 (users + credential
 * vault) so their shape is designed alongside the encryption model rather than guessed
 * ahead of it. Every change from here is a generated migration — never a hand edit.
 */

export {};
